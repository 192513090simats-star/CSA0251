#include <stdio.h>
int main()
{
    char ch;
    printf("Enter an uppercase letter: ");
    scanf("%c", &ch);
    ch = ch + 32;   // convert to lowercase
    printf("Lowercase letter = %c", ch);
    return 0;
}
