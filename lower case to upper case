#include <stdio.h>
int main()
{
    char ch;
    printf("Enter a lowercase letter: ");
    scanf("%c", &ch);
    ch = ch - 32;   // convert to uppercase
    printf("Uppercase = %c", ch);
    return 0;
}
